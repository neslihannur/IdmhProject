<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kullanıcı Girişi</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            margin: 0;
            display: flex;
            height: 100vh;
        }

        .image-container {
            flex: 1;
            position: relative;
            overflow: hidden;
        }

        .image-slide {
            position: absolute;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 1s ease;
        }

            .image-slide.active {
                opacity: 1;
            }

        .login-container {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: white;
            padding: 30px;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
        }

        .login-title {
            text-align: center;
            margin-bottom: 20px;
            font-weight: bold;
        }

        .footer {
            text-align: center;
            margin-top: 20px;
            font-size: 0.8rem;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="image-container">
        <img src="https://i.pinimg.com/236x/dd/74/12/dd74127fb2d737fa5f8340cf418a52b4.jpg" class="image-slide active" alt="Slide 1">
        <img src="https://i.pinimg.com/236x/32/a4/71/32a4715f0417e4585421efae99afcb0b.jpg" class="image-slide" alt="Slide 2">
    </div>
    <div class="login-container">
        <div class="login-title">
            <h2>Kullanıcı Girişi</h2>
        </div>
        <form>
            <div class="form-group">
                <label for="email">E-posta</label>
                <input type="email" class="form-control" id="email" placeholder="E-posta girin" required>
            </div>
            <div class="form-group">
                <label for="password">Şifre</label>
                <input type="password" class="form-control" id="password" placeholder="Şifre girin" required>
            </div>
            <button type="submit" class="btn btn-primary">Giriş Yap</button>
        </form>
        <div class="footer">
            <p>© 2024 Tüm hakları saklıdır.</p>
        </div>
    </div>

    <script>
        let currentIndex = 0;
        const slides = document.querySelectorAll('.image-slide');
        const totalSlides = slides.length;

        function showNextSlide() {
            slides[currentIndex].classList.remove('active');
            currentIndex = (currentIndex + 1) % totalSlides;
            slides[currentIndex].classList.add('active');
        }

        setInterval(showNextSlide, 3000); // Her 3 saniyede bir geçiş
    </script>
</body>
</html>
