@model IEnumerable<IdmhProject.Models.Project>

@{
    ViewData["Title"] = "Index";
    Layout = "_AdminLayout";
}



<p>
    <a asp-action="Create" class="btn btn-dark">Create New</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TeamMember)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Content)
                </td>
                <td>
                    @if (!string.IsNullOrEmpty(item.Image))
                    {
                        var images = item.Image.Split(',');
                        foreach (var image in images)
                        {
                            <img src="~/ProjectImages/@image" alt="@item.Title" style="width:100px; height:auto; margin-right: 10px;" />
                        }
                    }
                    else
                    {
                        <span>Fotoğraf yok</span>
                    }
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatedDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category.Name) // Kategori adını göster
                </td>
                <td>
                    <div class="btn-group-vertical">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-dark">Edit</a>
                        
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-dark">Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<style>
    .btn-group-vertical {
        display: flex;
        flex-direction: column; /* Butonları dikey dizme */
        gap: 5px; /* Butonlar arasındaki boşluk */
    }

    .btn {
        background-color: black; /* Buton arka plan rengi siyah */
        color: white; /* Buton yazı rengi beyaz */
        border: none; /* Kenarlıkları kaldır */
        padding: 5px 10px; /* İç boşluk */
        text-decoration: none; /* Alt çizgiyi kaldır */
        border-radius: 5px; /* Kenar yuvarlama */
        text-align: center; /* Yazıyı ortala */
    }

        .btn:hover {
            background-color: darkgray; /* Üzerine gelindiğinde arka plan rengi */
        }
</style>
